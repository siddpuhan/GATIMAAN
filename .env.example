# Root environment configuration template for GATIMAAN
# Copy to individual workspace packages as needed

NODE_ENV=development
PORT=4000

# PostgreSQL Database (Supabase PostgreSQL - configured via environment variables in Phase 2)
DATABASE_URL=
DIRECT_URL=
