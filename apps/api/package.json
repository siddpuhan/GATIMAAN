{
  "name": "@gatimaan/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src/",
    "typecheck": "tsc -b",
    "test": "node --test --test-reporter=spec || true"
  },
  "dependencies": {
    "@gatimaan/shared": "*"
  }
}
