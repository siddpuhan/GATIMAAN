{
  "name": "@gatimaan/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src/",
    "typecheck": "tsc -b",
    "test": "node --test --test-reporter=spec || true"
  },
  "dependencies": {
    "@gatimaan/shared": "*"
  }
}
